# TODO SEO

> Last updated: 2026-07-03

## Pending Changes

### Design a real Open Graph / Twitter card image (text + branding)
- **Source:** https://web.dev/articles/optimize-og-images / https://developer.twitter.com/en/docs/twitter-for-websites/cards/overview/summary-card-with-large-image
- **What:** `app/opengraph-image.png` was auto-generated by this pass (dark bg, red/green diff bars, 1200x630) purely to stop the previous broken/missing og:image. It has no title text, tagline, or logo — social previews will look like an abstract icon, not a branded card.
- **Where:** `app/opengraph-image.png` (and optionally `app/twitter-image.png` if a different crop/text is wanted — Next currently falls back to opengraph-image.png for Twitter, which is fine).
- **Why:** A designed image with "Text Diff Viewer — Compare Text Online Free" plus the diff-bar motif will meaningfully improve click-through when links are shared on social/chat apps.
- **Risk:** None to functionality — purely a visual asset swap. Needs actual design/graphic work (or a proper design tool) rather than the raw-pixel PNG encoder used to unblock this automatically.
- **Effort:** Low (swap the file once a real design exists).

### Consider a real favicon.ico for legacy user agents
- **Source:** https://web.dev/articles/favicon (favicon.ico still recommended as a fallback alongside modern PNG/SVG icons)
- **What:** `app/icon.png` (512x512, auto-detected by Next.js) now covers modern browsers, but very old browsers / some crawlers still look for `/favicon.ico` specifically.
- **Where:** `app/favicon.ico` (Next.js file convention — drop in and it's auto-linked, no code change).
- **Why:** Belt-and-suspenders favicon coverage; low priority since `app/icon.png` already handles all modern clients.
- **Risk:** None — purely additive file.
- **Effort:** Low.
